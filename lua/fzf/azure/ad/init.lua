return {
  apps = require("fzf.azure.ad.apps"),
  service_principals = require("fzf.azure.ad.service-principals"),
  users = require("fzf.azure.ad.users"),
  groups = require("fzf.azure.ad.groups"),
}
